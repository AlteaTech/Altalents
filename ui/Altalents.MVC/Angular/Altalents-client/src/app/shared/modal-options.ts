import { NgbModalOptions } from '@ng-bootstrap/ng-bootstrap';

// Options pour les modales "ajouter" (taille lg)
export const MODAL_OPTIONS_LG: NgbModalOptions = {
  backdrop: 'static',
  keyboard: false,
  size: 'lg',
};

// Options pour les modales "update" (taille xl)
export const MODAL_OPTIONS_XL: NgbModalOptions = {
  backdrop: 'static',
  keyboard: false,
  size: 'xl',
};